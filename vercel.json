{
  "buildCommand": "sh -c 'if [ \"$VERCEL_ENV\" = \"production\" ]; then npx prisma migrate deploy; fi && npm run build'",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 10
    }
  },
  "env": {
    "PRISMA_GENERATE_SKIP_DOWNLOAD": "true"
  }
}
