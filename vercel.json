{
  "buildCommand": "npx prisma generate && npm run build",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 10
    }
  },
  "env": {
    "PRISMA_GENERATE_SKIP_DOWNLOAD": "true"
  }
}
